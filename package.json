{
  "name": "voiceforge",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-based voice cloning, TTS, lip-sync preview, and virtual camera MVP for video calls.",
  "license": "MIT",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "react-joyride": "^3.1.0"
  }
}
